BIMQualityBlocksAdmin = function() {};
BIMQualityBlocksAdmin = BIMQualityBlocksAdmin.prototype = function() {};

jQuery( document ).ready( function() {
	jQuery( "#block-relations" ).chosen( {
		width: "600px"
	} );
	jQuery( "#block-deselects" ).chosen( {
		width: "600px"
	} );
} );

